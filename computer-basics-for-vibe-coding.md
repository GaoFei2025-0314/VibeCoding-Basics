# Vibe Coding 必备的计算机基础知识

这份笔记整理 Vibe Coding 过程中经常遇到的计算机基础概念，适合边做项目边学习。

## 一、文件和路径

要理解文件在哪里，以及程序从哪里读取文件。

```bash
pwd                 # 查看当前目录
ls -la              # 查看当前目录内容，包括隐藏文件
cd 项目目录          # 进入项目目录
find . -name "*.js" # 查找 JavaScript 文件
```

重点理解：

- 相对路径：`./src/app.js`
- 上一级目录：`../`
- 绝对路径：`/Users/gf/Documents/project`
- 隐藏文件：`.env`、`.git`

## 二、终端和 Shell

终端是和计算机沟通的重要工具。需要逐渐理解：

- 命令和命令参数，例如 `ls -la`
- 管道：`命令1 | 命令2`
- 重定向：`命令 > 文件`
- 环境变量
- 进程和端口
- 使用 `Ctrl + C` 停止程序

## 三、编程语言基础

不一定要一开始成为专业程序员，但要理解：

- 变量
- 条件判断
- 循环
- 函数
- 数组和对象
- 模块导入
- 异步操作
- 错误处理

理解这些内容，才能看懂 AI 生成的代码，而不是完全依赖 AI。

## 四、项目结构

常见项目文件和目录：

```text
README.md       # 项目说明
package.json    # JavaScript 项目配置和依赖
src/            # 源代码
public/         # 静态资源
.env            # 环境变量和密钥
.gitignore      # Git 忽略文件
```

## 五、包管理器和依赖

项目通常会安装别人已经写好的代码包：

```bash
npm install             # 安装项目依赖
npm run dev             # 启动开发服务器
npm run build           # 构建生产版本
```

也可能遇到 Python 和 macOS 软件安装命令：

```bash
pip install 包名        # 安装 Python 依赖
brew install 软件名     # 使用 Homebrew 安装 macOS 命令行软件
```

## 六、前端、后端和 API

一个网站通常可以简单理解为：

```text
前端页面 → 后端服务 → 数据库
```

需要逐步认识：

- HTML：页面结构
- CSS：页面样式
- JavaScript：页面交互
- API：前后端沟通接口
- HTTP：网络通信协议
- JSON：常见数据格式
- 请求和响应
- GET、POST、PUT、DELETE

## 七、数据库

做用户系统、记账、笔记、订单等功能时，通常会用到数据库。

重点理解：

- 表
- 行和列
- 主键
- 查询
- 新增
- 修改
- 删除
- 用户权限

常见数据库包括 SQLite、MySQL、PostgreSQL 和 MongoDB。

## 八、环境变量和密钥

很多项目会使用环境变量保存配置：

```text
API_KEY=你的密钥
DATABASE_URL=数据库地址
```

重要原则：

- 不要把密钥直接写在代码里
- 不要把 `.env` 提交到 GitHub
- 把 `.env` 加入 `.gitignore`
- 不要把真实密码发给 AI 或公开平台

## 九、调试和错误信息

遇到错误时，先看这些信息：

- 错误类型
- 出错文件
- 出错行号
- 调用栈
- 浏览器 Console
- 终端日志
- 网络请求状态码

常见 HTTP 状态码：

```text
200  # 请求成功
400  # 请求格式错误
401  # 未登录或身份验证失败
403  # 没有权限
404  # 找不到资源
500  # 服务器内部错误
```

## 十、部署和服务器

项目完成后，可能会遇到：

- 域名
- 服务器
- 环境变量
- 构建
- 部署
- 日志
- HTTPS
- 端口

简单理解：

> 本地开发是在自己的电脑上运行；部署是让别人通过网络访问项目。

## 十一、建议学习顺序

```text
文件和路径
→ 终端命令
→ Git
→ 编程基础
→ 项目结构
→ 包管理器
→ 前端和 API
→ 数据库
→ 调试
→ 部署
→ 安全
```

最先应该掌握的是：**看懂项目、运行项目、定位错误、保存版本、保护密钥。**
